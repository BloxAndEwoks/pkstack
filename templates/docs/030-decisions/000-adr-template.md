# NNN — <decision title>

<!-- Seeded by kstack-init. To write an ADR: copy this file to the band's next number
     (`docs/030-decisions/NNN-<slug>.md`), fill every section, delete the comments. An ADR
     records a DURABLE decision — structure, dependencies, interfaces, deployment, security,
     public contracts, non-functional requirements. The 030 band is the append-only decision
     LOG; the 020-architecture band is the living snapshot those decisions produce — when a
     decision lands, update 020; when it changes, supersede the ADR, never edit it. -->

Date: YYYY-MM-DD · Status: proposed | accepted | superseded by NNN

## Context

<!-- The forces that make this a decision at all: constraints, the problem, what breaks or
     stalls without one. Facts with provenance, not vibes. -->

## Decision

<!-- The decision itself, active voice, specific enough that a reader can tell whether later
     code conforms to it. -->

## Consequences

<!-- What becomes easier, what becomes harder, what is now forbidden. For every new invariant
     this decision creates: its writer set and its enforcement tier (a tier of "convention"
     needs the written reason here). -->

## Alternatives considered

<!-- The realistic alternatives across three lenses — professional (cleanest engineering),
     performant (fastest/cheapest at runtime), scope-necessary (smallest thing that serves the
     actual need) — and, for each, WHEN it would have won. A single-option ADR is a memo, not
     a decision record. -->
