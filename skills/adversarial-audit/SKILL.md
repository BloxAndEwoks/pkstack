---
name: adversarial-audit
description: "SUBSUMED by /verify-sweep — this skill is a pointer that routes onward. MANDATORY TRIGGERS: 'adversarial audit', 'adversarially audit', 'audit the suite', 'attack the guarantees', 'try to break this', 'break each guarantee', 'red-team the tests', 'is the suite actually proving this'. STRONG TRIGGERS: 'are these tests real', 'would this survive a real review', 'harden the tests', 'prove it end to end'. On any of these, invoke /verify-sweep. Do NOT trigger for a normal test-writing request or a factual question."
---

# Adversarial audit — subsumed by verify-sweep

`/verify-sweep` is the unit's verification, and it runs this same depth in the neutral
falsify/verify register: the composed-flow rule, the pre-registered commitment not to edit the
instrument it measures with, the reproduce-before-accepting bar, and the executed-counterexample
record.

The seam catalogue — the generic attack classes, the eight perf cost families, and the
deterministic race-proving recipe — now lives at
[`skills/verify-sweep/references/seam-catalogue.md`](../verify-sweep/references/seam-catalogue.md)
as optional reading a driver may consult, never a step and never a work breakdown.

Invoke `/verify-sweep`.
